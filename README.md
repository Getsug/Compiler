## compiler
This project contains the inital two phases of a compiler: scanner and parser.<br>

The scanner performs lexical analyis and outputs tokens whereas the parse receives the tokens and outputs a parse tree.<br>

#### NOTE:
**The parser is still buggy and might not perform perfectly as intended.**
